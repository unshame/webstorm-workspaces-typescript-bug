// Run `npm i` in the root folder first.
// Then start typing `axi` and press tab to verify that auto-import doesn't work
// Then, move "axios": "0.21.1" from apps/app/package.json to root package.json dependencies.
// Run `npm i` in the root folder and try again.
// This time the auto-import should work.
